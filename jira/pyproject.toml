[project]
name = "jira-mcp-server"
version = "0.1.0"
description = "MCP server for Jira Data Center JQL search"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
]

[project.scripts]
jira-mcp = "server:mcp.run"
