[project]
name = "confluence-mcp-server"
version = "0.1.0"
description = "MCP server for read-only access to Confluence pages and spaces"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
]

[project.scripts]
confluence-mcp = "server:mcp.run"
